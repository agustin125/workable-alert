{
  "name": "automation-workable-alert",
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.0.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.3",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "build": "tsc --build --clean && tsc",
    "start": "tsc && node dist/index.js",
    "local": "tsc && node dist/local.js",
    "dev": "ts-node-dev src/index.ts",
    "test": "jest --detectOpenHandles",
    "zip": "npm run build && zip -r automation-workable-alert.zip . -x '*.git*' -x 'src/*' -x '*.zip'"
  }
}
